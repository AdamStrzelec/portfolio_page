# Adam Strzelec portfolio page

Portfolio page built with react js

## Installation

Run `yarn` or `npm install` to install all the dependences.

Development

Run `yarn start` to build dev version of this app.

